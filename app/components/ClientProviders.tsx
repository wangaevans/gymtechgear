'use client'

import CookieConsentBanner from "./CookieConsentBanner";

export default async function ClientProviders() {
    return (
        <>
            <CookieConsentBanner />
        </>
    );
}